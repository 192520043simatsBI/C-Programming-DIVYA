#include<stdio.h>
void main()
{
    int a,b,i,j,f;
    scanf("%d%d",&a,&b);
    for(i=a;i<=b;i++)
    {
        f=1;
        for(j=2;j<i;j++)
        {
            if(i%j==0)
                f=0;
        }
        if(f&&i>1)
            printf("%d\n",i);
    }
}
