#include <stdio.h>

int main() {
    int n = 5, i, j;

    for(i = 1; i <= n; i++) {

        // spaces
        for(j = 1; j <= n - i; j++)
            printf(" ");

        // stars
        for(j = 1; j <= (2*i - 1); j++)
            printf("*");

        printf("\n");
    }

    return 0;
}
