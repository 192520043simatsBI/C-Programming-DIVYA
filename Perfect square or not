#include<stdio.h>
void main()
{
    int n,i,f=0;
    scanf("%d",&n);
    for(i=1;i*i<=n;i++)
    {
        if(i*i==n)
            f=1;
    }
    if(f)
        printf("perfect square");
    else
        printf("not perfect");
}
