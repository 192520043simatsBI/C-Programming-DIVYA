#include<stdio.h>
void main()
{
    int a,b,i,j,f;
    scanf("%d%d",&a,&b);
    for(i=a;i<=b;i++)
    {
        f=0;
        for(j=2;j<i;j++)
        {
            if(i%j==0)
                f=1;
        }
        if(f)
            printf("%d\n",i);
    }
}
